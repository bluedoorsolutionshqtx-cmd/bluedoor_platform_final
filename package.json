{
  "name": "bluedoor_platform",
  "private": true,
  "scripts": {
    "lint": "echo \"add lint\"",
    "test": "echo \"add tests\"",
    "build": "echo \"add build\""
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "ajv": "^8.18.0",
    "ajv-formats": "^3.0.1"
  }
}
